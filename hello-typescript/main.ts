var minhaVar = 'minha variavel';

function minhaFuncao(x, y) {
    return x + y;
}

// ES6
let num = 2;
const PI = 3.14;

var numeros = [1, 2, 3];
numeros.map(valor => valor * 2);

class Matematica {
    soma(x, y) {
        return x + y;
    }
}

var n1: string = 'abcdef';
